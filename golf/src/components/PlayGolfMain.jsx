import { StyleSheet, Text, View } from 'react-native';

const PlayGolfMain = () => {
  return (
    <View style={styles.container}>
      <Text>PlayGolfMain</Text>
    </View>
  );
};

const styles = StyleSheet.create({
  container: {
    height: '100%',
    justifyContent: 'center',
    alignItems: 'center',
  },
});

export default PlayGolfMain;
